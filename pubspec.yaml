name: mytime
description: Time→Choice Loop Conformance Test Pack (no UI)
version: 0.1.0

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  meta: ^1.12.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
